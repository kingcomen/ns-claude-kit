# /dev — inner loop implementation

1. Implement against the feature branch following CLAUDE.md coding standards.
2. Thin entry + fat modules. Naming prefix on all custom objects. @author Wichit Wongta.
3. After each change run `npm run inner` (lint + Jest). Iterate until green.
4. Do NOT deploy to SB here.
